---
:backends:
  - yaml
  - crypt

:hierarchy:
  - "clients/%{::clientcert}"
  - "machines/%{::domain}/%{::machine_type}"
  - "machines/%{::domain}"
  - "%{::lsbdistcodename}"
  - "%{::kernel}"
  - common

:yaml:
  :datadir: %{::confdir}/data

:crypt:
  :datadir: %{::confdir}/crypt
  :password_file: %{::confdir}/.password
