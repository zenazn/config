<%
domains = {}
emails = {}
@users.each do |user, data|
  data.fetch('email', []).each do |address|
    domain = address.rpartition('@').last
    domains[domain] = 'x'
    emails[address] = user
  end
end
(domains.sort + emails.sort).each do |key, value| -%>
<%= key %> <%= value %>
<% end %>
