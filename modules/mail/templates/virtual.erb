<%
domains = {}
emails = {}
@users.each do |user, data|
  data.fetch('email', []).each do |address|
    domain = address.split('@', 2).last
    domains[domain] = 'x'
    emails[address] = user
  end
end
[domains.sort, emails.sort].flatten.each do |key, value| %>
<%= key %> <%= value %>
<% end %>
